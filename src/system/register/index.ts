// Public surface of the pulsar register layer. The companion CSS file
// `./tokens.css` must be imported once at workbench bootstrap (see
// `src/main.ts`) so the custom properties are defined on `:root`.

export { CSS_TOKEN_NAMES, color, gsapDuration, motion, space, z } from './tokens';
