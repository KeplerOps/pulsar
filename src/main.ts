// Workbench entry point. The runtime mount lands once the scene contract
// (PUL-F001) and the workbench mode dispatch (PUL-A008) are in place.
// Until then this file exists so Vite has an entry to build.
export {};
