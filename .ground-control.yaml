schema_version: 1
project: pulsar
github_repo: KeplerOps/pulsar
workflow:
  test_command: pnpm test
  completion_command: pnpm lint && pnpm typecheck && pnpm test
  lint_command: pnpm lint
  format_command: pnpm format
sonarcloud:
  project_key: KeplerOps_pulsar
  organization: keplerops
rules:
  plan_rules: .gc/plan-rules.md
knowledge:
  dir: docs/knowledge
